export var senderID = null;
export var userName = null;
export var password = null;

export function initSMSConfig(senderIDV, userNameV, passwordV) {

    senderID = senderIDV;
    userName = userNameV;
    password = passwordV;
}